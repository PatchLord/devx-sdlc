> **FICTIONAL — part of a simulated engagement.** Marigold Living does not exist. See `../../README.md`.

> Annexe to the Statement of Work v1.0, 11 August 2026. The signature document is `../sow.md`;
> where the two differ, **the signature document governs**.

# Annexe A — Acceptance criteria

Every criterion, with the artefact that proves it. Referenced from §7 of the signature document.

## How the work is accepted

The rule that carries the most weight in this document: **every acceptance criterion names the artefact that will prove it.** A criterion with prose in that column fails a release rather than passing it. "Verified" is not evidence, and neither is "we tested it manually".

The criteria live in the repository on a protected path with named owners. An agent may propose a change to one. Only a named person approves one. Criteria an agent can edit to match what it built are not criteria.

**The criteria added at v1.0 are appended rather than renumbered.** A-1 to A-17 still mean what they meant in v0.9, so anything already written against them still resolves; the new phase-1 rows run from A-18, and phase 2's five original rows moved to A-40 to A-44 to make room. Where a v0.9 criterion promised more than its artefact showed, it has been narrowed rather than left broad, and the four corrections are listed by name after §7.3 so a reader comparing the two versions can find them.

### 7.1 Phase 1

| # | Criterion | The artefact that proves it |
|---|---|---|
| **A-1** | A product whose stock crosses below its floor produces exactly one open task, with the product, the reading and the time on it | An automated test over a hand-synthesised store response, plus a **replay of a real reading sequence from the ledger through the live reader in dev**, with the resulting task row beside the ledger rows that produced it. Plus a screenshot of the board after the crossing caused by **C-13** — a named person at Marigold adjusting one product's stock in the admin on 18 September. We are read-only by X-1, so we cannot cause a crossing ourselves; that is why the event is somebody's dependency and not our assumption |
| **A-2** | The same product staying below its floor across several readings does not produce a second task while the first is open | Automated test with a sequence of readings; the ledger extract showing the readings that were taken. The behaviour itself — one task, not two — is Priya's decision recorded in **D-14**, not a thing settled by whoever wrote this test |
| **A-3** | The count of orders past dispatch promise is correct against a stated calendar, visible any day, and covers exactly the orders it claims to cover | A query, committed in the repository, run against Marigold's real orders, with its output next to the same count computed by hand from a Shopify order export for the same day and the two agreeing — **and, because both of those read the same Shopify fulfilment timestamp, a third check against something outside Shopify: courier pickup scans or the 3PL's own timestamps for twenty orders.** Without the third, this criterion verifies the SQL and not the clock, which is the tautology Karan demolished in Marigold's own test 1. The screen states in words which orders it covers — India only, or India and UK — and which calendar it counts on, per **D-12** |
| **A-4** | Priya can assign a task to one person, and no task acquires a name any other way | A browser test that drives the actual screen: opens an unassigned task, sees the proposed name, confirms it, and sees it assigned. Plus a test asserting that no task reaches assigned state without a confirming user id |
| **A-5** | The assignee, and only the assignee, is messaged on assignment | A test capturing outbound messages for a five-person fixture and asserting exactly one recipient, plus a screenshot of the message as it arrives on a phone. Content per **A-25**, timing per **A-26** |
| **A-6** | A person can complete a task; the completion records who and when; Priya can put it back; both events survive | A browser test performing complete-then-reopen, plus the two database rows it produced |
| **A-7** | Rakesh can see the board, complete a task assigned to him, and add a task, on the device and network profile in A-27 | A recorded browser session at **A-27's** profile covering exactly those three actions, plus one screenshot from Rakesh's actual handset **taken by 15 September** — three weeks before last merge, so failing it is fixable. Named fallback if the handset cannot be got to on the day: the closest device we can buy against D-8's answer, the substitution written into the release document, and the real-handset screenshot taken in the pre-window fortnight as a check rather than as the criterion. Narrowed from v0.9's "a full day's work", which no artefact here demonstrates |
| **A-8** | Floors: ~60 authored by hand, every other product covered by its category default, 200 new SKUs inheriting automatically | A query showing every product resolving to a floor and where each came from, run before and after a batch of new SKUs is created |
| **A-9** | Bulk edit changes forty floors in one submission and reports what it changed | A browser test that submits forty changes and asserts forty rows changed, plus the screen's own confirmation output |
| **A-10** | The reading ledger is append-only, and no scheduled job removes or moves rows out of it | A test asserting that update and delete against that table fail, plus the migration itself under code-owner review, **plus the repository's own job inventory showing no archive or purge job touching that table** — the second half of v0.9's "never archived" had no artefact against it and now has one |
| **A-11** | Authorisation, on top of a valid session (A-18, A-19): every signed-in user sees every task — a decision, per assumption 7, not an accident; only the assignee or Priya completes; only Priya or her named backup assigns or reopens; a temp account has exactly the same shape as a permanent one and loses its access the moment it is deactivated | One test per role per action, listed exhaustively rather than as "permissions work", plus a browser test where a temp account attempts to complete someone else's task and is refused, plus the deactivation case in A-19. **v0.9's A-11 began at "everyone sees every task", which answered a question nobody had asked** — that visibility is authorisation, and authorisation is meaningless without the boundary in A-18 |
| **A-12** | Marigold owns the repository, the hosting and every third-party account | Screenshots of the ownership pages, and one page live in dev put there by the pipeline with its build id matching a tagged commit. Liveness where four real people actually work is **A-37**, not this row |
| **A-13** | Floors typed in the sheet arrive on the board on an explicit import, and the board reads back what it understood | A browser test importing a fixture sheet containing one deliberately malformed value, and the read-back screen showing it |
| **A-14** | The mirror tab is written by the machine, and no cell in it is a source of truth | The written sheet after a run, and a test asserting the import path reads only the floors range |
| **A-15** | The 08:00 digest is sent once a day, lists what is outstanding and who has it, and goes nowhere else | A test asserting one send per day for a fixture, plus a screenshot of a real digest. Content per A-25, delivery time per A-26. Channel per **D-2, whose fallback is in force at issue** — individual messages, not a group message, unless D-2 says a group can be addressed |
| **A-16** | Completed tasks leave the screen after seven days and remain queryable | A browser test with dated fixtures, plus a query returning the hidden rows |
| **A-17** | The reopen view reports, for a chosen week, what was marked done and later put back, by person | The view's output next to the same figures computed by hand from the underlying rows for that week |
| **A-18** | **Every route the application serves refuses a request with no session, and returns nothing** — no data, no partial page, and no error message containing a task, an order number or a person's name | A test that enumerates the routes **from the router itself** rather than from a hand-written list, calls each with no session, and asserts a refusal and an empty body. The enumeration is regenerated on every run, so a route added in October without a session check fails this test instead of being missed by it. Plus the same test run against the production URL on the day of go-live, with its output in the release document |
| **A-19** | **The sign-in boundary holds and a session ends.** A wrong credential is refused; a session expires after a stated idle period and a stated absolute period; deactivating an account ends that account's live session | A browser test that signs in, is refused with a wrong credential, signs out, and is refused again after the clock is advanced past each of the two periods; plus a browser test where a named person deactivates a temp account and the temp's already-open tab is refused on its next request. **Both periods are written in the design document with the reason for the number**, not left to a framework default — and the deactivation half is what makes §10.4's promise about temp accounts a mechanism rather than a hope |
| **A-20** | A floor crossing becomes an open task inside a stated maximum: **20 minutes between 08:00 and 22:00 IST, 65 minutes outside it**, both derived from D-3's interval | A test measuring the elapsed time between the ledger row that recorded the crossing and the task row's creation time, run against the live reader in dev, with both timestamps in the output. The number is also on the screen, because without a maximum "a task appears by itself" is satisfied by a nightly poll and Priya's February happens again in a new costume |
| **A-21** | **A scheduled reading that did not start is noticed** | An alarm that fires when no reading cycle has completed inside twice its interval, **with the alarm's own test run**: the reader stopped in dev on purpose, the alarm fired, and the notification captured. The whole board hangs off this reader; in v0.9 nothing in §7 noticed it dying |
| **A-22** | **A reading cycle that processed zero rows is noticed** | Alarm plus its own test run, driven to fire against a store response containing no products. A silent no-op is the most expensive way for this to fail, because the board looks calm |
| **A-23** | **A reading cycle that ran twice is noticed, and the second run does not double the ledger or the tasks** | Alarm plus its test run on a deliberate overlap, plus the ledger extract from that overlap showing one row per product per cycle |
| **A-24** | **A reading that failed and could not be retried is noticed rather than dropped** | The failed-reading queue's own alarm on depth above zero, with its test run, plus the row a deliberately poisoned reading left in it |
| **A-25** | **Personal data and free text do not leave the application.** Request logs carry an allowlist of fields; error reports are built from the same allowlist; an outbound message carries a task reference and an order number and never the free-text body | Three artefacts, one per channel, because a client reads one sentence about three channels at one strength: a test asserting the logger drops every field not on the allowlist; a test asserting the error reporter's payload is composed from the same allowlist, with one captured payload in the output; and a test asserting a message body is composed from reference fields only, run against a fixture whose free text contains a phone number, with the resulting message showing it absent. **What this criterion does not cover is stated in §10.4 in its own words: keeping that field out of a model prompt is an instruction to four named people, not a mechanism, and D-5 is what closes it** |
| **A-26** | **No message is delivered into a recipient's night.** A message raised outside 08:00–21:00 in that recipient's declared local time is held and released at 08:00 local | A test with two recipients on declared IST and UK offsets asserting the hold and release time for each, including across a UK clock change, plus the two held-then-released rows. Anjali (care) works UK evenings; a digest at 08:00 IST reaches her at 02:30, and the one person whose muted phone costs Marigold a customer is the one v0.9's notification design was not written for |
| **A-27** | **The device and network profile is written down, and the board works on it** | The profile itself — make, class, browser, screen size, throttled bandwidth and latency, named in the design document from D-8's answer — plus a recorded browser run of see-board, complete-own-task and add-task at that profile with the throttle settings visible in the run output. v0.9 promised behaviour at the signal quality of the Bhiwandi unit and had no criterion of any kind behind it |
| **A-28** | An unassigned task older than a stated age is flagged on the board | A browser test with dated fixtures showing flagged and unflagged, plus the query behind the flag. Priya is the only person who can confirm an assignment, so during Diwali her button is the board's throughput limit; this is a mitigation, not a fix, and the risk is in §15 |
| **A-29** | **A person can add a task in their own words, from the board, and it appears** | A browser test that clicks the button, types into the field, submits, and finds the task on the board under the author's name. The screen, not the endpoint. Our own pilot shipped nine write paths and zero buttons because its check could only see whether a page rendered, and §4.2 tells Marigold that story — a free-text form with no browser test named against it is the same mistake with the story printed above it |
| **A-30** | **An order passing its dispatch promise produces exactly one open task**, with the order number and the elapsed clock on it | An automated test over a hand-synthesised order shape, plus the task row produced by the live reader in dev against an order that has passed, plus that order's row in the reading ledger. A-3 demonstrates that the *count* is right; nothing in v0.9 demonstrated that the *task* appears, which is what P1-3 actually promises |
| **A-31** | Priya can answer "what is unfinished right now and who has it" from one screen, without asking anyone | A recorded session of **Priya herself** doing it on a working day's real data with the elapsed time visible in the recording, taken twice — once during build and once in the pre-window fortnight. Dev's fourth test is a minute; a criterion that does not time it is a criterion about a screen existing |
| **A-32** | **S-1's measurement exists as a document with its method attached**, not as an opinion | The measurement report committed in the repository: inventory-change frequency on the moving SKUs, what the orders payload actually contains, and the UK fulfilment delay across ~90 real orders — each with the query that produced it and the date it ran. Per §10.6: run by **Karan Iyer by hand, with no agent session**, field-limited at the query, and **every fixture in the repository hand-synthesised from the payload's shape rather than a captured real payload** |
| **A-33** | **S-2's shapes are fixed, in writing, on the protected path, before implementation tickets exist** | The permission model, the definition of a task, the definition of a floor, and what starts and stops the dispatch clock, committed under `docs/design/criteria/` with code-owner approval and Priya's written acceptance. These are the shapes four people's work sits on; code can be rewritten in an afternoon and this cannot |
| **A-34** | **The board can be restored** | The backup configuration committed in the repository with a recovery point of 24 hours or better; the restore path written down as steps a person can follow without asking us; and **one restore actually performed into dev with the elapsed time recorded.** X-11 declines the production restore *drill*, and in v0.9 that declension let the absence of backups hide behind it — the board is the sole record of the eight weeks Dev's verdict needs |
| **A-35** | **The read budget exists as a number and something fires when it is crossed** | The budget written in the design document with **Karan Iyer** as owner, agreed with Dev before 25 September; plus an alarm on reads per hour crossing it, with its own test run. This is the only control on the one way an internal tool can hurt Marigold's customers |
| **A-36** | A message the provider rejected is noticed | An alarm on the provider's own failure status, with its test run against a deliberately rejected send. Delivery to the provider, not delivery to a human — N-4 |
| **A-37** | **The release running where four real people work is the artefact the pipeline promoted** | The build identifier visible on the board in production matching a tagged commit and a pipeline run, captured on the day it goes live, with the release document stating that no build step sits between the artefact QA approved in uat and the artefact serving production. A-12 proves a page in dev; this proves the environment the team actually uses, named in §5.4 |
| **A-38** | **The design brief is accepted by Priya in writing before implementation tickets exist** | The brief itself, naming the empty, loading, error and validation state of every screen and every breakpoint down to A-27's profile, with Priya's signature and its date. Changing a frame costs minutes; changing a built screen costs a ticket |
| **A-39** | A person who has never seen the board can be shown how to use it from something we hand over | One page of instructions and one recorded walkthrough, in the repository, covering add, assign, complete, reopen and floors. This is a deliverable, not a claim about Dev's fifteen-minute test — N-9 stands, and teaching the temps is still Marigold's per X-20 |

### 7.2 Phase 2

**Before the first phase-2 ticket, §4's four questions are asked again rather than carried forward.** Standard was set against a board that only reads, on the public internet behind a login. Phase 2 adds an endpoint anyone on the internet can post to, which creates work for seven people — that is a different answer to *what breaks and who feels it*, and it may be a different depth. Karan Iyer re-answers the four in writing at the start of phase 2, and the answer goes to Dev before any of it is built. **The receiver's path joins the protected set in §5.1 alongside the notification path**, and A-47 is what demonstrates it.

| # | Criterion | The artefact that proves it |
|---|---|---|
| **A-40** | A due date set by Priya displays as the correct local moment to a user in India and to a user in the UK, including across a UK clock change | A test table of cases with expected values **computed by hand**, plus two screenshots of the same task under two timezone profiles |
| **A-41** | A reminder is sent once and only once, including when the sender is retried or runs twice | A test that invokes the send path twice and asserts one message; the idempotency record it wrote |
| **A-42** | No reminder is sent for a task already completed | A test with a completion landing between scheduling and sending |
| **A-43** | A live signal from the store produces a task, and a signal that arrives twice does not produce two | A test against a hand-synthesised payload replayed twice, plus the ledger rows distinguishing live from scheduled. This is idempotency only; authenticity is A-45 and A-46, and in v0.9 nothing covered it |
| **A-44** | A live signal that is missed or delayed is visible rather than silent | An alarm that fires when no signal has arrived for longer than its expected interval, with the alarm's own test run |
| **A-45** | **A payload arriving without a signature, or with a signature that does not verify, is rejected before anything is created — and the rejection is counted** | Three tests: an unsigned payload, a payload signed with the wrong secret, and a payload whose body was altered after signing. Each asserts no task, no ledger row, and the rejection counter incrementing. Plus an alarm on that counter rising, with its own test run, because a forged payload arriving repeatedly is somebody trying rather than a glitch |
| **A-46** | **A payload that verifies but is stale is rejected, including a byte-identical replay of one we already accepted** | A test replaying a correctly signed payload with its timestamp moved past the freshness window and asserting rejection; a second test replaying it inside the window and asserting the idempotency record in A-43 refuses the duplicate. The window's length is written in the design document with the reason for the number |
| **A-47** | **The receiver's code was reviewed by a second code owner, and the review actually happened** | The pull request itself, showing two owner approvals on the receiver's path, and that path present in the ownership file — an ownership pattern matching nothing is owned by nobody while still reading as protected, which is why the artefact is the approval and not the file |

### 7.3 How Marigold accepts a release

In v0.9, acceptance was a property of our own paperwork, signed by our own QA. That is structurally the same thing as a "QA phase" with no artefact behind it, which is the line on an old invoice Dev is still angry about. So there is a client side to it, and it is short enough to use.

| Step | What happens |
|---|---|
| **1. We offer the release** | With its document: what was checked, how, the artefact, and what this release does not verify. **Every criterion in the release has a non-empty artefact cell or the release is not offered.** Imran Shaikh signs that the artefacts are the artefacts, which is a devx signature and is not acceptance |
| **2. Marigold accepts, at the right level** | **Priya Menon signs for behaviour. Dev Rawat signs for anything touching data or store access** — the same split as §9.1, so nobody is asked to accept something outside what they decide |
| **3. Three working days** | Priya may extend once, by three more, by saying so. No form |
| **4. A rejection names the criterion** | And what about the artefact does not satisfy it. That is a rejection we fix inside the price. "It does not feel right" is a legitimate thing to want and a different thing to want: it is a change request under §12, priced, not argued about |
| **5. Silence accepts** | If nothing arrives inside the window and no extension was asked for, the release is accepted. **That is deemed acceptance and it is Anjali Sharma's term, disclosed rather than buried** — a release accepted by silence in Diwali week, inside Dev's own three-to-four-day window, is the risk it creates, and Priya's single extension is the whole of the mitigation. It is in §15 with my name on it |

*Corrections made in this section between v0.9 and v1.0, named so a reader comparing the two can find them: **A-7** promised "a full day's work" and now says what its artefact shows; **A-10** promised "never archived" with nothing behind that half and now names the job inventory; **A-3** was broader than a hand-check that read the same Shopify timestamp as the machine, and now carries a check against something outside Shopify; **A-7's cross-reference to A-8** — the stock-floors criterion — is corrected to **A-27**, the device and network profile. The same wrong reference to A-8 appears in §2.1's P1-1 and is corrected there, in that section. A-11's opening clause was not a wrong reference but a wrong assumption, and it is rewritten above.*

---
